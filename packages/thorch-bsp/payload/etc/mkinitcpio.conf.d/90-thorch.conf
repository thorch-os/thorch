# Thorch boot policy. Local administrators can override this in a later
# lexically ordered drop-in without modifying package-owned defaults.
MODULES+=(btrfs)
HOOKS=(base udev modconf kms keyboard keymap consolefont block thorch-firmware thorch-sd-prefer filesystems fsck)
